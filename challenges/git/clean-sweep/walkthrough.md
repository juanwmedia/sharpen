git clean -nd first: it lists what would be swept (build.log, notes/, tmp/) without touching anything. Then git clean -fd: -f actually removes untracked files, -d extends the sweep to untracked directories. git clean never touches tracked files, so the modified src/api/client.ts keeps its changes. Plain rm -rf on the junk paths is equally valid: the arena checks the resulting state, not the commands.
