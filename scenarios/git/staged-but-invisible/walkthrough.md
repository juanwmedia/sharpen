Plain `git diff` is quiet because every change is already in the index. `git diff --staged` (same as `--cached`) shows what the next commit would ship: the auth fix and `notes.local`. Unstage only the scratch file with `git restore --staged notes.local` so it stays on disk untracked. The arena checks the resulting index, not whether you ran the diff; the diff is how you see the trap.
