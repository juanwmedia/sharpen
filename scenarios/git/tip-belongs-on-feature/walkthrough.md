Create the label while you still stand on the tip: `git branch feature/dark-mode`. Then `git reset --hard HEAD~` moves `main` (and the worktree) back one commit. Hard reset is deliberately destructive of the current tip's tree on this branch; the feature branch still points at the dark-mode commit. Soft reset would leave the theme files staged on main, which is not what you want here.
